#set document(title: [Page not found])

#metadata((
  summary: "The page you requested does not exist.",
  pdf: false,
)) <website-metadata>

#title()

That page does not exist. Go back to the #link("/index.html")[notes index].
